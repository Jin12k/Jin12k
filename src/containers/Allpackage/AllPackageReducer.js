import Constants from './AllPackageConstants';

let initialState = {
  leadId: {},
};

// export default function AllPackageReducer(state = initialState, action) {
//   console.log('action.data' + JSON.stringify(action.data));
//   let newState = Object.assign({}, state);

//   switch (action.type) {
//     case Constants.ACTIONS.ALL_PACKAGE_TXT_SRCH:
//       return (newState.leadId = action.data);
//       break;
//     default:
//       break;
//   }
//   return newState;
// }
